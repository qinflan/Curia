import { Text, View } from "react-native";

export default function NotificationsInbox() {
  return (

    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Edit this to create notifications inbox react component.</Text>
    </View>
  );
}