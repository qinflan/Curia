const jwt = require("jsonwebtoken")

// on login/signup check provided token, if no jwt token present, then generate an access token

function verifyAccessToken() {

}

module.exports(verifyAccessToken())